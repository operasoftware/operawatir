#!/bin/sh
jruby -S rspec --color $1

##!/usr/bin/jruby

#require 'rubygems'
#require 'rbconfig'
#require 'rspec'

#spec.configuration.color_enabled = true unless Config::CONFIG['host_os'] =~ /mswin|msys|mingw32/

#version = ">= 0"
#gem 'rspec-core', version
#load Gem.bin_path('rspec-core', 'rspec', version)

